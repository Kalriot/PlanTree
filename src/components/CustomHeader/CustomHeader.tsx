import { Box, Text } from '@mantine/core';

export default function CustomHeader() {
  return (
    <Box style={{ display: 'flex', alignItems: 'center', height: '100%' }}>
      <Text>Header Arriba (Standalone)</Text>
    </Box>
  );
}
